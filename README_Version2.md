## Boiler-managed code (DO NOT EDIT)

This repository syncs shared boilerplate code from `orgpb/boiler` into the `boiler/` directory.

Rules:
- Do not manually edit files under `boiler/` (they will be overwritten by sync).
- Put repo-specific changes under `overrides/` (or other non-`boiler/` folders).